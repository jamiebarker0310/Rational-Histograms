language: python
python:
  - "3.6"      # current default Python on Travis CI

install:
  - pip install -r requirements.txt
  - pip install .
# command to run tests
script:
  - python3 rationalHistogram/tests/test_rational_histogram.py